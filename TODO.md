ui
* cable details could be on right floating panel, not under the camvas
* allow right devices to be dragged and dropped, moved out of the way
* let enter does not create a cable right away with prompt open
* default to cat6 or a logical default for link (eg power, fiber, etc)
* hide devices with no connections
* show devices (rught side) with most connections first
* power ports should be reversed, pdn/outlets on left, power ports
* allow delete key to delete selected cable buttom

* allow multi select, for delete

bigger
* allow multi create cable, like in qsys: 
* display icons insteaf of circles in patch, follow netbox method for this
